#include "qiu.h"
#include <stdio.h>

void greet(const char* name) {
    printf("Hello, %s! Welcome to Qiu ORM.\n", name);
}

void executeQuery(const char* query) {
    printf("Executing Query: %s\n", query);
}
